const styles = (theme) => ({
    main: {
        marginTop: 25,
        display: 'flex',
        justifyContent: 'center',
    },
    headerContent: {
        width: '50%',
        display: 'flex',
        justifyContent: 'center',
        flexDirection: 'column'
    },
    carousel: {
        margin: '20px 0px 20px 0px',
        height: 500,
    }

});

export default styles;
